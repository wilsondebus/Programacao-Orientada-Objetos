public interface Animal{

    //dentro da interface tudo é public e abstract 
    //declarar atributos apenas se for uma constante 
    public void emitirSom();
    void exibirInfo(); 
    boolean verificarVacinacao(); 
}